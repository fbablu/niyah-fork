# Documentation Audit — 2026-05-19

> Snapshot of staleness across all 21 `.md` files in the repo. Generated by cross-referencing every concrete claim (file paths, function names, dependency versions, counts, deployment states) against the live codebase on `groups` branch at `7164297`.
>
> Treat this as a triage list, not a fix script. Items below are quoted from the docs as they stood on 2026-05-19; some may have been corrected by the time you read this.

## Files audited

```
CLAUDE.md
PLAN.md
README.md
docs/architecture.md
docs/cleanup.md
docs/development.md
docs/features.md
docs/group-equity.md
docs/lane-b-apple-targets-migration.md
docs/legal.md
docs/may-16-progress.md
docs/may-17-plan.md
docs/native-modules.md
docs/payments.md
docs/post-demo-roadmap.md
docs/qa.md
docs/roadmap.md
docs/security.md
docs/sprint-april15.md
docs/ui-animation.md
docs/ui-ux-remediation-plan.md
```

## Cross-cutting drift

These same wrong numbers/names propagate across many docs. Fix once, then sweep references.

| Stale claim | Actual | Appears in |
|---|---|---|
| `1018 tests, 48 suites` | **821 tests (815 passed + 6 skipped), 47 suites** | CLAUDE.md:18, README.md:85, development.md:58, roadmap.md:25, ui-animation.md:89 |
| `24 Cloud Functions deployed` | **33 exports in `functions/src/index.ts`** | CLAUDE.md:52, architecture.md:83, development.md:121, security.md:14, payments.md:13, roadmap.md:32/68, may-17-plan.md:13, ui-animation.md:83 |
| `Group session: 7 CFs` | **~10 (`createGroupSession`, `respondToGroupInvite`, `markOnlineForSession`, `startGroupSession`, `reportSessionStatus`, `reportShieldViolation`, `cancelGroupSession`, `autoTimeoutGroupSessions`, `sessionProgressNudges`, `distributeGroupPayouts`)** | CLAUDE.md:118, features.md:69, roadmap.md:70, sprint-april15.md:152 |
| `9 FCM notification types` | **~7 distinct `type` values in source; counting depends on how forgiveness/leaderboard are counted** | CLAUDE.md:118, features.md:67, roadmap.md:71 |
| `app.config.ts` | **`app.config.js`** | CLAUDE.md:93, architecture.md:102/126 |
| `scripts/dev-device.sh` reference | **Deleted 2026-05-19 (this commit)** | architecture.md:93 |
| `withScreenTimeEntitlement.js`, `withDeviceActivityMonitor.js` plugins | **Removed in Lane B; `@bacons/apple-targets` now owns entitlements + extension registration via `targets/*/expo-target.config.json`** | CLAUDE.md:53, architecture.md:85-91, native-modules.md:48-60 |
| Live Activities, DeviceActivityReport "planned / Phase 4" | **Shipped 2026-05-16 (`targets/widget/`, `targets/report/`); JS bridge in `src/config/screentime.ts:238-260`** | native-modules.md:75, roadmap.md:42, post-demo-roadmap.md (Lane B section) |
| Account linking "Phase 4" | **Shipped (`src/utils/accountLinking.ts:70` uses `linkWithCredential`; `mergeDuplicateUsers`/`requestAccountMerge` CFs deployed)** | roadmap.md:41, post-demo-roadmap.md (gap matrix) |
| FamilyControls Distribution "pending submit for `device-activity-report`" | **All 5 extension IDs approved 2026-05-16 per may-16-progress.md:20** | roadmap.md:49,54, lane-b-apple-targets-migration.md:41, may-17-plan.md:14 (says 4, should be 5) |
| App Check "biggest remaining gap, needs Firebase Console setup + `context.app` checks" | **Fully wired client-side (`src/config/appCheck.ts`) and server-side (`assertAppCheck` helper at `functions/src/index.ts:121`); 7 money-path CFs hard-enforce when `APP_CHECK_ENFORCED=true`. Gap is the env flip + token coverage, not the implementation.** | security.md:81, roadmap.md:36, payments.md:40 |

## Per-file findings

### CLAUDE.md

**Critical:**
- line 18: `Jest + jest-expo (1018 tests)` — actual 821.
- line 52: `24 deployed` Cloud Functions — actual 33.
- line 53: plugins list mentions `Firebase, Screen Time, entitlements`; Screen Time + entitlements plugins were removed in Lane B. Current plugins: `withFirebaseStaticFrameworks.js`, `withFmtConstevalFix.js`, `withFollyCoroutinesFix.js`, `withGoogleServicesJson.js`, `withGoogleServicesPlist.js`, `withResourceBundleSigning.js`.
- line 93: `app.config.ts` — actual `app.config.js`.

**Medium:**
- line 108: `Final Sprint — Demo Day April 15, 2026` — demo shipped April 15; current phase is App Store launch by May 26.
- line 127: `Key remaining work (5 days)` list — every bullet (Stripe/Plaid live, FamilyControls approval) is already done.
- line 118: notification type count 9 — code has ~7 distinct types.

### README.md

**Critical:**
- line 85: `1018 tests` — actual 821.

### PLAN.md

All checked claims verified.

### docs/architecture.md

**Critical:**
- line 71: `ScrollContext.tsx` listed under `src/hooks/` — file does not exist. Hooks dir has only `useColors.ts`, `useCountdown.ts`, `useScreenProtection.ts`.
- line 83: `24 deployed` — actual 33.
- lines 85–91: plugins list still includes the removed `withScreenTimeEntitlement.js` and `withDeviceActivityMonitor.js`; missing `withFmtConstevalFix.js` and `withResourceBundleSigning.js`.
- line 93: `dev-device.sh` listed — deleted.
- line 102, 126: `app.config.ts` — actual `app.config.js`.

**Medium:**
- `(auth)/` listing missing: `how-it-works.tsx`, `intake.tsx`, `screen-time-math.tsx`, `screentime-baseline.tsx`.
- `session/` listing missing: `verify-identity.tsx`.
- Root `app/` missing: `blocked.tsx`, `screentime-priorities.tsx`.
- `src/config/` missing: `appCheck.ts`, `sentry.ts`.
- `src/store/` missing: `featureFlagsStore.ts`, `resetCoordinator.ts`.
- `src/constants/` missing: `blobAvatar.ts`.
- `src/utils/` missing: `accountLinking.ts`, `analytics.ts`, `errors.ts`, `id.ts`, `otpThrottle.ts`.
- Missing top-level `targets/` directory (apple-targets: `monitor`, `report`, `shieldaction`, `shieldconfig`, `widget`) — load-bearing for understanding Lane B.

### docs/development.md

**Critical:**
- line 58: `1018 tests, 48 suites` — actual 821, 47 suites.
- line 121: `24 Cloud Functions deployed` — actual 33.
- lines 123–148: CF table missing at least: `unlinkBankAccount`, `replaceBankAccount`, `getWithdrawalEligibility`, `reportShieldViolation`, `sessionProgressNudges`, `aggregateDailyMetrics`, `requestAccountMerge`, `mergeDuplicateUsers`, `reconcileWalletBalances`.

**Minor:**
- `pnpm clean` and `pnpm clean:deep` exist in `package.json` but aren't documented in the Commands section.
- (Note: the device-dev section was rewritten 2026-05-19 — that part is current.)

### docs/native-modules.md

**Critical (entire doc heavily affected by Lane B):**
- line 31–33: `DeviceActivityMonitorExtension.swift`, `ShieldActionExtension.swift`, `ShieldConfigurationExtension.swift` claimed under `modules/niyah-screentime/ios/`. Actual: moved to `targets/monitor/`, `targets/shieldaction/`, `targets/shieldconfig/` respectively. Only `AppPickerHostingController.swift`, `NiyahActivityAttributes.swift`, `NiyahScreenTimeModule.swift` remain in the module.
- lines 48–60: `withScreenTimeEntitlement.js` and `withDeviceActivityMonitor.js` referenced — both deleted. `@bacons/apple-targets` handles entitlements + target registration via `targets/*/expo-target.config.json` + `generated.entitlements`.
- line 75: Lane B premium-UX features framed as "still pending" — shipped 2026-05-16.
- lines 78–98: `NiyahDeviceActivityReport` section says `ios/NiyahDeviceActivityReport/DeviceActivityReport.swift`. Actual: `targets/report/DeviceActivityReport.swift`.
- line 98: plugin `withScreenTimeExtensions.ts` — does not exist; extensions registered via `@bacons/apple-targets`.
- lines 102–127: `NiyahLiveActivity` section says bridge is "new `NiyahLiveActivityModule.swift` or extended `NiyahScreenTimeModule.swift`". Actual: `startLiveActivity` / `endLiveActivity` are on existing `NiyahScreenTimeModule.swift:395,511`. Widget files at `targets/widget/NiyahLiveActivityWidget.swift` + `NiyahLiveActivityBundle.swift`.

**Recommendation:** rewrite this file end-to-end against current `targets/` + `modules/niyah-screentime/`.

### docs/features.md

**Critical:**
- lines 134, 143, 154: Demo Mode section frames Stripe trust model (virtual balances, settle outside app) as the current operating model with "real Stripe escrow planned for Phase 2". Actual: Stripe live keys are deployed; `DEMO_MODE` only short-circuits Cloud Function calls in dev. Non-demo runs on live Stripe Connect + Plaid prod. Phase 2 backlink (`#phase-2-beta-cohort`) is a dead anchor in `docs/roadmap.md`.

**Medium:**
- line 69: `7 Cloud Functions deployed` for group sessions — actual ~10 (see cross-cutting table).
- line 67: `9 FCM push notification types` — actual ~7-10 depending on counting method.
- line 85: shield variant detection claims branching on `application.token` and `category.token`. Actual: `targets/shieldconfig/ShieldConfigurationExtension.swift` uses `application.bundleIdentifier` and `category.localizedDisplayName` via `detectVariant(bundleID:categoryName:)`.
- line 85: `20+ rotating quotes keyed by Calendar.minute` — actual selection is `Int(Date().timeIntervalSince1970 / 60) % quotes.count`, not `Calendar.minute`.

### docs/ui-animation.md

**Critical:**
- line 91: `Stripe | Payments | Per-transaction | Integrated (test mode)` — actual live mode.
- line 33: `(tabs)/_layout.tsx` claimed to use `Animated.sequence` bounce on tab icons. Actual: file uses `<Tabs>` from `src/components/BottomTabs` (`react-native-bottom-tabs`, native UITabBarController) — no JS animation.
- line 21: `expo-linear-gradient` listed as `Installed, not used in any component`. Actual: used in `app/blocked.tsx:11` and `app/(auth)/welcome.tsx:13`.

**Medium:**
- line 22: `react-native-svg | 15.15.3` — package.json:88 has `^15.15.5`.
- line 83: `24 functions deployed` — actual 33.
- line 89: `1018 tests passing` — actual 821 (39 suites with test files).

### docs/ui-ux-remediation-plan.md

**Critical (Phases 1, 3, 4 mostly shipped, checkboxes still unchecked):**
- line 196: references `@invertase/react-native-apple-authentication` — NOT installed. Code uses `expo-apple-authentication` (`app/(auth)/auth-entry.tsx:13`). Self-contradicts line 21 which says "use the official Apple sign-in control".
- lines 105–139 (Phase 1): `AuthScreenScaffold`, `SessionScreenScaffold`, `LegalAcceptanceOverlay`, `LegalContentView`, `acceptLegalTerms` CF, `legalAcceptanceVersion`/`legalAcceptedAt` — all already exist (`src/components/*Scaffold.tsx`, `src/components/Legal*.tsx`; `functions/src/index.ts:19`).
- lines 237–238 (Phase 3): friends `ScrollView`→`FlatList` and `InviteCTA` extraction — both done. `app/(tabs)/friends.tsx:6` imports `FlatList`, `src/components/InviteCTA.tsx` exists.
- lines 291–296 (Phase 4): `resetCoordinator.ts` — exists in `src/store/resetCoordinator.ts`.

**Medium:**
- line 110: scaffold migration target list missing newer screens in `app/session/`: `bank-setup.tsx`, `invites.tsx`, `quick-block.tsx`, `verify-identity.tsx`, `waiting-room.tsx`.
- line 105: scaffold migration list missing `(auth)/` screens: `how-it-works.tsx`, `intake.tsx`, `phone-entry.tsx`, `screen-time-math.tsx`, `screentime-baseline.tsx`, `screentime-setup.tsx`, `verify-phone.tsx`, `welcome.tsx`.

**Recommendation:** mark whole doc as shipped/archived; checkbox UX reads as outstanding work.

### docs/legal.md

**Critical:**
- lines 36–37: "Phase 1: Honor-based settlement with Venmo deep links. No money transmission. Phase 2: Stripe escrow…" — Stripe escrow is **live** (group session payouts via `distributeGroupPayouts` + Stripe Connect; roadmap.md:59 confirms live keys deployed; April 2026 campus launch processed real escrow). Honor-based / Venmo framing is the pre-April-15 model.
- line 73: outstanding action `Terms of Service and Privacy Policy` — `src/components/LegalContentView.tsx` exists and is wired via `acceptLegalTerms` CF. Acceptance flow shipped; remaining work is lawyer-reviewed final copy.
- line 75: outstanding action `Commitment contract disclaimer in app` — `src/components/LegalContentView.tsx:28` already contains: "Niyah is not a gambling, gaming, lottery, or betting service. Stakes are commitment devices…"

**Medium:**
- line 49: terminology guidance ("Avoid: bet, wager, gamble, win") is broadly followed but `app/(tabs)/friends.tsx:826` SMS template uses "win". Either fix the copy or update the rule to allow "win" in social/referral context.

### docs/security.md

**Critical:**
- line 14: `All 24 Cloud Functions` — actual 33.
- line 81: App Check "biggest remaining gap" with implementation pending. Actual: fully wired via `src/config/appCheck.ts` + `functions/src/index.ts:41` `APP_CHECK_ENFORCED` flag + `assertAppCheck` helper at line 121. Seven money-path CFs hard-enforce when the flag is on: `createPlaidLinkToken`, `linkBankAccount`, `unlinkBankAccount`, `replaceBankAccount`, `requestWithdrawal`, `createGroupSession`, `requestAccountMerge`.

**Medium:**
- lines 51–54: Secret Manager list omits `PLAID_CLIENT_ID`, `PLAID_SECRET`, `ADMIN_API_KEY`. `GCLOUD_PROJECT`/`GCP_PROJECT` is not a Firebase Secret (auto-set runtime env var).
- line 23: Firestore rules description is materially incomplete. Missing collections governed by current rules: `rateLimits`, `revenue`, `transactions`, `groupSessions`, `groupInvites`, `analytics_events`, `metrics`, `config/featureFlags`, `userMerges`, `migrations`, `walletAudits`.
- line 25: `users — owner read/write` — actual: any signed-in user can read any user doc (`request.auth != null`, rules:16). Owner-write is gated by an `affectedKeys().hasAny([...])` blocklist for KYC/Stripe/phone/email/merge fields (rules:28–40), not blanket owner-write.
- line 26: `wallets — owner read only, no client writes` — actual: rules allow owner `create` with `balance == 0 && pendingBalance == 0` (rules:67–72).
- line 27: `sessions — owner read/write with field validation` — actual: client `update` restricted to `['status', 'completedAt', 'updatedAt']` (rules:127); financial fields immutable.
- line 28: `userFollows — owner write` — actual: `allow write: if false` (rules:92); follow/unfollow goes through `followUserFn`/`unfollowUserFn` CFs.

### docs/payments.md

**Critical (money-path docs — high stakes):**
- line 13 + table 15–26: `24 Cloud Functions deployed` — actual 33. Table omits the Plaid stack (`createPlaidLinkToken`, `linkBankAccount`, `unlinkBankAccount`, `replaceBankAccount`), `getWithdrawalEligibility`, `reconcileWalletBalances`.
- line 106: Withdrawal idempotency key documented as `withdrawal:${userId}:${requestedAtMs}:${amount}:${method}`. Actual at `functions/src/index.ts:2527`: `withdrawal:${uid}:${amount}:${method}:${idemBucket}` where `idemBucket = Math.floor(Date.now() / 60_000)` (60-second bucket). Replay-safety reasoning based on the documented key would be wrong.
- line 105: Solo session payout idempotency key `solo_session_payout:${sessionId}:${userId}:${amount}` — no such key exists. Solo settlement (`handleSessionComplete:1939-2073`) credits wallet via Firestore transaction; no `stripe.transfers.create`.
- lines 148–169: Group Session schema diverges from `src/types/index.ts:235`:
  - `hostUid` → actual `proposerId`
  - `participantUids` → actual `participantIds` (and `participants` is `Record<string, GroupSessionParticipant>`)
  - `status` listed `"proposed" | "ready" | "active" | "completed" | "cancelled"` — actual: `"pending" | "ready" | "active" | "completed" | "cancelled"` (`pending`, not `proposed`)
  - Missing required fields: `cadence`, `customStake`, `duration`, `poolTotal`, `autoTimeoutAt`, `createdAt`, `updatedAt`
  - `results: { [uid]: { completed, screenTimeMs? } }` — no separate `results` map; completion lives on `participants[uid].completed`
  - GroupInvite block: `fromUserId`/`toUserId` (not `inviterUid`/`inviteeUid`), uses `stake` not `stakeAmount`, status type includes `"expired"`
- line 91: `reconcileWalletBalances` listed under post-demo Lane D5 as if unbuilt — deployed at `functions/src/index.ts:5247`, schedule `0 4 * * * America/New_York`. Sentry-alert claim overstated: code uses `console.error` only; no Sentry binding in `functions/src/`.

**Medium:**
- line 39: `createAccountLink` redirect URLs "use `process.env.GCLOUD_PROJECT` (dynamic)" — actual: hardcoded `https://niyah.live?stripe=refresh` and `https://niyah.live?stripe=return` (lines 1167–1168).
- line 40: App Check Lane A2 reference stale (already enforced on 7 paths).
- line 56: `unlinkBankAccount` / `replaceBankAccount` "post-demo Lane D1+D2" — both deployed 2026-05-14 (`functions/src/index.ts:1606,1730`).

**Minor:**
- line 124: "minimize the number of peer-to-peer transfers" — `payoutAlgorithm.ts:79` self-admits "may produce more transfers than the theoretical minimum".

### docs/group-equity.md

Forward-looking design doc; correctly framed as design, not state.

**Medium:**
- line 73: references `assertWithdrawalEligibility` — actual exported name is `getWithdrawalEligibility` (`functions/src/index.ts:~2240`).
- line 88: `distributeGroupPayouts` consuming raw `usedMs` from extension snapshot — actual consumes pre-stored `sessionData.payouts` map. Future implementation needs a producer step.

### docs/roadmap.md

**Critical:**
- line 8: `Current Status (May 13, 2026)` — today is 2026-05-19, 6 days stale. Misses Lane B (5/16) and May 26 launch deadline.
- line 25: test count 1018 — actual 821.
- line 31: Push Notifications "foreground via notifee (Lane C5)" claimed in-flight — shipped (`src/config/notifications.ts:228-270`).
- line 36: App Check "Soft-fail today; flipping to enforce on auth-related CFs in Lane A2" — money paths already hard-enforce; only env flag flip remains.
- line 41: Account Linking "Phase 4" — shipped (`src/utils/accountLinking.ts`, `mergeDuplicateUsers`, `requestAccountMerge`).
- line 42: Live Activities "Phase 4" — shipped 2026-05-16 (`targets/widget/`).
- line 49: FamilyControls Distribution "submitted April 10, pending Apple review" — approved 2026-05-16 for all 5 extension bundle IDs.
- line 54: FamilyControls for `device-activity-report` "Pending submit" — already part of the 5-ID approval.
- line 68: 24 Cloud Functions — actual 33.
- line 70: 7 group-session functions — actual ~10.
- line 141: `reconcileWalletBalances` "In progress" — deployed.

**Medium:**
- line 82+ Phase 4 status pills don't reflect that most lanes shipped.
- lines 104–113: Lane A/B/C/D scope descriptions read as if most lanes are unstarted. Reality: only A1, A2 flip, B3, B4, C1/C2/C3/C4/C6, D5/D6/D7 remain substantively open.

### docs/sprint-april15.md

Correctly framed as historical (line 1). Internal counts stale (7 group CFs, 9 push types) but acceptable for archive.

### docs/post-demo-roadmap.md

**Critical:**
- line 8: `Today is 2026-05-05` — actual 2026-05-19.
- line 22 gap matrix: most "Code does" entries reflect April state, not May 17+ state. Account linking, group session modals, active timer UI, Live Activities, Plaid bank change — all moved on.
- Lane A: A3, A4, A5, A6 shipped per commits `202c569`, `085571d`.
- Lane B: entire lane shipped via apple-targets migration 2026-05-16 (commits `81f44ef`, `455a28c`, `7164297`). Only B3 (baseline onboarding screen) and B4 (per-app shield variants) deferred.
- Lane C: C1 (`StatusBanner`), C4 (scrubber Timer), C5 (foreground notifee) shipped. Only C3 leaderboard polish, C6 push types, C7 optimistic payout substantively open.
- Lane D: D1, D2, D3, D4, D5 shipped. Only D6 instrumentation review and D7 polish remain.
- line 367: `functions/test/withdraw-earned.test.ts` — actual `functions/src/withdraw-earned.test.ts`.

### docs/lane-b-apple-targets-migration.md

**Critical:**
- line 41: "submit if not already approved for `com.niyah.app.device-activity-report`. (Other 4 already approved per memory.)" — all 5 approved as of 2026-05-16.

Otherwise content verified against `targets/`, `package.json` patched deps, `app.config.js` entitlements.

### docs/may-16-progress.md

All checked claims verified at time of writing (2026-05-16). Now 3 days old; "T-10 days" is now T-7.

### docs/may-17-plan.md

**Medium:**
- line 13: 24 Cloud Functions — actual 33.
- line 14: FamilyControls Distribution "approved 2026-04-09 for main app + 4 extensions" — should be 5 extensions (May 16 approval).
- line 3: "T-9 days" was correct on May 17; today is T-7.

### docs/qa.md

Stub file (26 lines, trails off mid-section). No concrete shipped/todo claims to verify.

### docs/cleanup.md

All checked claims verified.

## Triage suggestions

**Surgical (~15 min):**

- `CLAUDE.md:18` `1018` → `821`
- `CLAUDE.md:52` `24` → `33`
- `CLAUDE.md:53` plugin list rewrite (drop Screen Time/entitlements, add new ones)
- `CLAUDE.md:93,108,127` rewrite Current Phase block + `app.config.js`
- `README.md:85` `1018` → `821`
- `docs/development.md:58,121` counts; expand CF table to 33
- `docs/architecture.md` — sweep counts, file ext, plugins, deleted script, missing files
- `docs/may-17-plan.md:13,14` — counts, FamilyControls extension count
- `docs/post-demo-roadmap.md:367` — fix test path

**Rewrite per file (~60 min):**

- `docs/native-modules.md` end-to-end (Lane B moved everything)
- `docs/legal.md` Phase 1/Phase 2 → "Pre-April-15 vs current" with escrow live
- `docs/features.md` Demo Mode payments section + Phase 2 anchor
- `docs/security.md` App Check section + Firestore rules collection list
- `docs/payments.md` CF table + GroupSessionDoc schema + idempotency keys

**Reorganize (~30 min):**

- `docs/roadmap.md` — move shipped lanes out of in-flight columns
- `docs/post-demo-roadmap.md` — mark Lane B fully shipped, update gap matrix to May 17+ state
- `docs/ui-ux-remediation-plan.md` — mark Phase 1/3/4 shipped or archive whole doc

**Out of scope:**

- `app/(tabs)/friends.tsx:826` SMS uses "win" — either fix copy or relax `legal.md:49` terminology rule for social/referral context
